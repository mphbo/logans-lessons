// Arrays []

// Accessing items of arrays ex) myArray[1]

// For loop

const newArray = ["hey", "hows", "it", "going", "friend"];

for (let i = 0; i < newArray.length; i++) {
  console.log(newArray[i]);
}

// console.log(newArray.length);

// For of loop

for (const item of newArray) {
  //   console.log(item);
}

// console.log(animal);

// for in loop

for (let key in animal) {
  console.log(animal[key]);
}

// .forEach

// .map()

// .filter()

// .find()

// Challenges

// Use a for loop to loop through an array and print each string to the console: ['hello', 'there', 'my', 'name', 'is', 'russell'];

// Use a for of loop to loop through an array and add the array [2, 3, 8, 4, 5] together;

// Get the instances of the numbers 1, 8 and 9 from the array: [2, 3, 3, 5, 1, 3, 2, 8, 8, 1, 9, 3, 4, 1, 9];

// Map over an array and create a string from each number in the format 'this is the number (number goes here)' [3, 9, 4, 1];
